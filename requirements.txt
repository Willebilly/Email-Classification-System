# My Mail Classifier Requirements
# Last updated: July 2026

# Core dependencies
numpy>=1.24.3			# numerical operations
pandas>=2.0.3			# data manipulation
scikit-learn>=1.3.0		# Random forest and metrics
xgboost>=1.7.6			# Gradient boosting

# Test processing ( with Swedish support )
beautifulsoup4>=4.12.2		# HTML parsing
lxml>=4.9.3			# Faster HTML parsing (compatible with beautifulsoup)
nltk>=3.8.1			# NLP utilities

# Swedish language support
# Snowball stemmer is built into NLTK, but we need Swedish data
# The stopwords for Swedish will be downloaded by NLTK

# Utilities
joblib>=1.3.2			# Model persistence
python-magic>=0.4.27		# Attachment detection
charset-normalizer>=3.2.0	# Debugging for .mbox + better email encoding detection (handles weird charsets)

# Visualization (might change later idk
matplotlib>=3.7.2
seaborn>=0.12.2